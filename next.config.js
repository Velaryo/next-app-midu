/** @type {import('next').NextConfig} */
const nextConfig = {
  // para permitir uso de rutos
  // nueva forma
  experimental: {
    appDir: true
  },
  reactStrictMode: true
}

module.exports = nextConfig
